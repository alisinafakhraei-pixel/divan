import { notFound } from "next/navigation";

// Temporarily hidden along with /community/ask — see git history for the full question detail page.
export default function QuestionDetailPage() {
  notFound();
}
