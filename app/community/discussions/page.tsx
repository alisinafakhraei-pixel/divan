import { notFound } from "next/navigation";

// Temporarily hidden along with the rest of /community — see git history for the full Discussions page.
export default function DiscussionsPage() {
  notFound();
}
