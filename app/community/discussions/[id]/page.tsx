import { notFound } from "next/navigation";

// Temporarily hidden along with /community/discussions — see git history for the full thread page.
export default function DiscussionThreadPage() {
  notFound();
}
