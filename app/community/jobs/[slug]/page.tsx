import { notFound } from "next/navigation";

// Temporarily hidden along with /community/jobs — see git history for the full job detail page.
export default function JobDetailPage() {
  notFound();
}
